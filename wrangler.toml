name = "flowbooks"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# NEXT_PUBLIC_* vars are baked in at build time via .env.production
# Server-side secrets should be set in Cloudflare Dashboard > Pages > Settings > Environment Variables
